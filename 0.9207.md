# 0.9207 待办清单

来源：`D:\Wiki\BUG反馈清单7.xlsx`（清单5/6 同一张表的续收：老行 #2–#84 原样、行号一致，其中 #69 反馈人补了 9206 二次测试备注、#84 整行改写；新增 #85–#160，#101、#153 缺失，加上仍缺的 #55、#72，共 74 条新数据行）。收集时间约 2026/9/2–9/5：9/2 当天的批次绝大多数报的是 0.9205 线上版（多条明写「9205」），9/4 起出现 0.9206 报告（#69 备注、#155、#158、#159）；时间戳列继续混杂序列值与手填文本（#93/#116/#134/#139 的序列值明显错年），以内容中的版本号为准。本文行号一律指清单7；引用上一代结论时写作「0.9205 #N」「0.9203 #N」。

发版时间线（git）：`build.txt` 8/31 升 0.9203、9/1 升 0.9205、9/2 14:16 升 0.9206、9/4 03:27 升 0.9207；0.9206 更新日志见 [0.9206.md](0.9206.md)。凡 9/2 报在 9205 上、而修复提交落在 0.9206 区间内的条目，本文按「已修复核」挂出，等 9206 真机回归。

与 0.9205 的关系：清单7 里 #2–#84 的裁定全部沿用 [0.9205.md](0.9205.md)，本文不再重抄；只有 #69 与 #84 两行内容有变，作为新数据点挂到对应条目下。新增行里同因/同族的报告挂到既有条目，其余立目。本轮全部新条目做了静态源码核对并挂「核对（9/5）」子行（附图 7 张逐张核对锚点行号，关键信息并入描述）；未编译、未动代码，真机验收统一待做。

统计：新增 74 行里，已修复核 8 条（9205 报告，修复随 0.9206 发布），同因挂靠 16 条，新立目 33 条（35 行，#97/#139、#110/#123 两组并案；其中静态确证根因 7 条：#132 / #154 / #157 / #116 / #144 / #88 / #158），建议愿望 8 条，无效 / 设计如此 / 提问 / 信息不足 6 条，文案 1 条（#108）。

---

## 一、崩溃 / 卡死 / 报错（最高优先）

- [ ] 用鬼切神威疾走时突然卡住、能听到音效、随后崩游戏（#97；同报：#139「鬼切突进的时候会闪退」，灾厄+大修，稳定复现）
  - 核对（9/5）：崩溃级、两条独立报告但都无日志。疾走链是 `OniFlashStep`（弹幕）+ `OniFlashMark`（墨痕）+ 隐身旗 `LocalPlayerHidden`（0.9205 #19 已把切世界残留收掉），静态没有裸露的越界/空引用；「卡住还能听到音效」说明主线程还在跑一段（音效由 SoundEngine 线程播），更像渲染层（RenderHandle/RT）或粒子层的异常被吞后连锁。需 client.log 末尾异常栈；回访是否装了灾厄之外的模组、是否在领域内、疾走穿的是什么目标
- [ ] 多人联机进入鬼伞世界（鬼雨）：主机进入后崩溃，随后整个地图区块的方块全部消失；非主机只是退出重连（#125，灾厄+大修+熵灾，稳定复现；反馈人自判多半是与熵灾的联机冲突）
  - 核对（9/5）：崩服级。「区块方块消失」不是本模组任何路径会写的东西，是 SubworldLibrary 在主机崩溃后回主世界时区块未加载/未回写的表现，根因在崩溃本身。鬼雨世界的进出走 `KiameWorld.EnterWorld → KiameGuard.Snapshot`（灾厄旗标与城镇 NPC 快照，绑世界 ID）与 `KiamePlayer.OnEnterWorld → RestoreOnReturn`，快照体系静态无联机专属分支。需主机 server.log/client.log 异常栈；先排熵灾（反馈人自判），再看 SLib 联机多人进子世界是否本就不受支持的组合
- [ ] 多人模式死神永生打肉山：鬼切与鬼伞同时开领域、同时攻击大量饿鬼时，战斗中成员的游戏崩溃（#128，传恶死神，稳定复现）
  - 核对（9/5）：崩溃级、无日志。饿鬼是肉山的附属 NPC（残酷肉山重制 `TheHungryAI` 接管），两域同开 + 大量目标 + 成员端崩，与 0.9205 #68「鬼切鬼伞同开领域打光女反复崩服」同一场景族：本机能想到的叠乘点是鬼伞湖体对大量 NPC 的抓取判定、鬼切面影（`OniOmokage`）逐 NPC 建纸偶、两套天空/RT 抢占。需成员端 client.log 异常栈
- [ ] 开残酷打肉墙会直接卡住（#109，常态，稳定）
  - 核对（9/5）：「卡住」无法分辨是游戏卡死还是 Boss 卡住不动。残酷肉山重制在 `BrutalWallOfFlesh/`（本体 + 眼 + 饿鬼 + 抓取运镜 `WofGrabCutscene`），抓取运镜走 InnoVault Cinematics 全程 `SetControls` 清操作（`WofGrabPerformancePlayer`），若运镜异常中断玩家会被钉住。待回访：是人物不能动还是肉山不动、是否伴随红字、单人还是联机
- [X] 不礼貌地抢走收集者基站已锁定、准备拿取的物品时，左下角报错，不影响游玩（#96，附图：红字「ERROR: An Empty Transfer Occurred! The Value of Item.type is Zero!」连刷四行，收集者阵列+电网；反馈人注仅加载大修也稳定复现）
  - 已修复核：与 0.9205 #40 同一报错、同一根因（物品被玩家拾走后槽位换成空实例、臂仍对旧槽位取 `.CWR()`）。0.9205 第一批（9/2）已修：`CollectorArm` 追踪/抓取每帧先过 `TrackedItemIntact`（同引用、同类型、`Alives()`）再取 CWR，修复随 0.9206 发布（0.9206 #13）。本条报的是 9205，待 9206 真机复测「玩家抢走被锁物品」这一面
- [X] 9205 开旧存档后左下角一直有零数量物品吸入报错，只在旧档出现，开新档没有（#99）
  - 已修复核：「吸入」即收集者磁吸，报错文本同 #96/0.9205 #40。旧档特有的原因在已修根因里有对应面：磁吸记的是 `item.whoAmI`，tML 在 `NewItem_Inner` 从不写该字段，同帧新生成的掉落物上是旧值 0，臂追到错误槽位；旧档里的收集者阵列通常已在持续出货，新档还没有。0.9206 已把搜索与磁吸改按槽位索引遍历。待 9206 真机开旧档看红字是否消失

## 二、BossRush / Boss 战流程

- [ ] 0.9206 修罗模式用鬼伞打 BossRush：打完烬甲秽魔后一直重复它的出场音效，Rush 进度不再推进，身边一直有蓝色像护盾一样的东西（#158，灾厄熵+大修+众神，偶尔）
  - 核对（9/5）：「烬甲秽魔」是灾厄熵的 Apsychos（其本地化 `DisplayName: 烬甲秽魔`），在灾厄 BossRush 名单里。灾厄 `BossRushEvent.Update` 的规则是：**场上没有 boss 时倒计时到零就再召当前阶段的 boss（默认 1 秒）并播出场音，阶段只在 `OnBossKill` 里推进**。「一直重复出场音效、进度不推进」逐字就是「当前阶段的 boss 被移除了但没有经过 OnKill」的循环。本模组里恰有这样一条路径：鬼伞沉溺对已击败过的 boss 整组直接移除真身（`KikasaDrown`，不走死亡），而 `KikasaLegend` 全目录零处引用 BossRush（只有试炼文案），沉溺门槛 `KikasaBossGate` 只看「本世界击败过」不看事件是否进行中；Apsychos 是模组 boss，走世界击杀台账，杀过一次即可沉。蓝色护盾状物待回访（可能是反复重刷的 boss 出场特效）
  - 拍板项：BossRush 进行中沉溺一律转鞭笞（与 0.9203 #47「BossRush 豁免档位增幅」同口径），或至少对 BossRush 名单内的 boss 拒收；另需回访反馈人是否真在 Apsychos 上用了沉溺
- [ ] 0.9206 BossRush 在史莱姆之神一关卡住无法继续（#159，9/5）
  - 核对（9/5）：与 #158 同为 BossRush 卡停；史莱姆之神一关的推进条件是核心真死。可疑面两条：①同 #158 的沉溺移除（核心/圣卫已击败过即可沉，圣卫被移除不算死，核心不解锁）；②#130 反馈人自查的「鬼切领域导致再召唤时不生成两只圣卫」，若属实，核心在 BossRush 里永不解锁。本模组对史莱姆之神本身零接管，0.9203 #57 替死鬼秒圣卫已修。待真机 BossRush 打到史神看圣卫是否出现、是否用过沉溺
- [ ] 灾法双开受虐死神：被史莱姆之神击败后再次召唤，不生成两只圣卫；反馈人自查确定是鬼切的领域导致（#130，稳定复现；表内批注同文）
  - 核对（9/5）：静态在鬼切领域侧找不到任何 NPC 生成拦截或移除路径。全仓 `GlobalNPC.OnSpawn` 五处（世界冻结、面影身份令牌、信标塔改址、教程靶子、时缓对齐）没有一处会把新生 NPC 置失活或隐藏；领域天空/浸染是整屏调色不裁实体，面影只挂纸偶不动真身（0.9203 #20 同结论）。圣卫是灾厄 `SlimeGodCore` 在 AI 里 `NewNPC` 出来的，若领域开着时核心 AI 被冻结（鬼切处决/改铭台的世界冻结）它就不会出圣卫，但解冻即续。待真机按反馈人步骤复现：死后再召时领域是开着的还是刚开、是否恰在处决/改铭台时停里召唤、圣卫是否其实在场只是被画在别处
- [ ] BossRush 里用朗基努斯之枪攻击头顶光环的星流巨械（已打巨械之怒）时，出现锁血与贴图抽搐（#148）
  - 核对（9/5）：朗基努斯之枪是本模组武器（`LonginusHeld`/`LonginusThrow`）；「星流巨械」是灾厄 Exo Mechs，「头顶光环」应指 Ares 或 Thanatos 的部件。灾厄的机械三体在部分阶段本就 `dontTakeDamage`（只能打当前激活体），「锁血」是它自身的阶段机制；「贴图抽搐」若只在被枪命中时出现，可疑面是朗基努斯的命中效果（击退/顶位移）作用在体节链上。待回访：是打哪一体、不用这把枪是否也抽、是否联机
- [ ] 饕餮之喉 × 灾厄 NPC 消失簇：携带饕餮之喉打灾厄之影二阶段的两个小弟时，其中一个突然消失，打死另一个也不回来，Boss 也打不了；换下饕餮之喉即恢复正常（#127，附图两张：灾厄之影 51809/143436 血条卡住，场上只剩一只小弟；稳定复现）
  - 同族：0.9205 #24「9202 旱海狂鲨被饕餮之喉影响会直接消失」（当时备查待 9203 复测）；#89「灾厄+大修+辅助模组，在沙漠用召唤物召唤瀚海狂鲨基本直接脱战」未提饕餮但同为灾厄 NPC 提前离场，并入备查
  - 核对（9/5）：饕餮之喉的舌攫（`GluttonousThroat.SelectFarthestTarget`）每 9 秒攫取范围内**最远的可追敌人**，没有任何 boss/boss 部件豁免，舌弹幕在服务端权威改写目标位置把它拖到脸前并挂拖拽冻结租约。灾厄 `Cataclysm/Catastrophe` 的自毁条件只有一条：`CalamityGlobalNPC.calamitas` 索引失效（灾厄之影不在场），静态下拖拽不会碰这个索引（灾厄只在槽位失活或类型变化时重置它）；旱海狂鲨则在目标玩家死亡或距离 >5600px 时把 `timeLeft` 压到 10 自毁。两条静态都对不上「被拖一下就消失」，但三条反馈都指向同一件饰品，先按饰品复现：真机带饕餮打灾厄之影，看小弟消失前一帧是否正被舌头拖着、client.log 有无异常
- [ ] 世吞沉溺簇：用鬼伞拖拽世界吞噬者时出现身子丢失、只剩一个头乱飞，仍需打 Boss 战，未测二次能否抓到头（#123）；用鬼伞打世吞血量降到一半以下后无法收入伞内（#110）
  - 核对（9/5）：整组收集 `NpcGroupHelper.CollectGroup` 对世吞有 ai 双向链补集（注释明写为「EoW 分裂半截也能收满整条」），但原版蠕虫 AI（aiStyle 6）的规矩是：体节的前/后邻一旦失活，该体节**就地变形为新头/新尾（type 改变）**。沉溺按「40 帧短持有后在抓握节拍移除真身」逐个移除，只要不是同一帧全部消失，残余体节就会在中途变成新头，按类型追踪的沉溺目标失配、被遗漏，读作「身子没了剩个头乱飞」。#110 的「半血后收不进」疑同源：半血后世吞已多次分裂、槽位与类型重排，请求解析时的资格判定（`IsEligibleTarget`/门槛身份 `IdentityTypeOf`）容易落在一节刚变形的体节上。修法方向：世吞类整组移除改为同帧一次性置失活（先全部标记再统一移除），或沿用 0.9205 #75 的思路把分裂型蠕虫列进沉溺黑名单转鞭笞。待真机
- [ ] 击杀 boss 后的过场动画中人物不无敌，容易被小怪打死（#114，稳定复现）
  - 核对（9/5）：反馈没说是哪段过场。全仓击杀后会锁操作的过场有：日真礼物（妖刀铭文拓本）叙事、鬼奴沉溺演出、残酷 boss 各自的死亡演出运镜。鬼伞重启演出全程 `player.immune = true`（`KikasaReset` 倒放帧逐帧顶住），是有无敌的；叙事类过场（`NarrativeRouter`）只锁输入不给无敌。待回访是哪段过场（大概率是 boss 死后弹出的对话/礼物演出），拍板是否给叙事过场加短无敌或允许移动
- [ ] 死亡追踪同根簇新数据：鬼切在没打过 boss 时也会触发对话和奖励（#106）；打 boss 没打过死了，但触发了击败对话，任务却显示未完成，难度全开（#124）
  - 回指：0.9205 #3/#12（炼狱下玩家被杀反而拿到谢礼/击败对话，红茶注顺从归档）。#124 没提炼狱，「难度全开」指本模组四档；本模组死亡监听族（`DeathTrackingNPC` 子类）把 boss 消失当真死的面在没有炼狱时也可能出现：残酷 boss 的脱战/放弃状态（`*DespawnState`）若走 `life=0; checkDead()` 收尾，就会烧到监听。待回访是哪只 boss、是否残酷、是否联机
- [ ] 分别按般若、千手、梵鐘的条件打 boss，打完不掉铭刻，进度也没加（#144，灾厄+大修，稳定复现）
  - 核对（9/5）：**静态确证一条系统性漏洞**。三枚铭刻都是「了结」信道的刀縁（`OniMeiDeedEvents.NotifyKill`，由鬼切七处招式的 OnHitNPC 在 owner 端调用），调用点与入口都要求那一击打完后主体已死（`!target.active || target.life <= 0`）。而残酷重制 boss 的 `CheckDead` 普遍先锁血演出（`KingSlimeAI` 即典型：`life = 1; dontTakeDamage = true; 切 DeathState`，boss 目录近五十处同款锁血写入），真死发生在死亡状态收尾里、由 boss 自己 `life=0; checkDead()`，不是任何一刀打出来的。于是在残酷世界里，鬼切的最后一刀看到的永远是 `life == 1`，Kill 事件根本不发，了结信道的縁（般若「一成血斩首领」、千手「终结送走八种首领」、梵鐘「全程不用灭世/终结敲下首领」以及同信道其它縁）在重制 boss 上一枚都结不成；同一道门后的 `TryPetalPruneOnKill`（断首击杀返还架势等）在这些 boss 上同样永不触发。有死亡动画的灾厄 boss 同病。与 0.9203 #44 腐环「假死帧烧闩」是同一类问题，那次的修法是在真死收尾处幂等补登记；刀縁需要同样一条「真死回填」入口（记住最后一刀的持有者与当时快照，死亡演出真死时补发 Kill 事件）。待拍板修法后实现
- [ ] 文案补全：鬼切要求击杀「首领」解锁铭刻，哪些算首领没有说清，史莱姆之王、硫磺火元素似乎不算（#108）
  - 核对（9/5）：刀縁的首领判定是 `OniMeiDeedTargets.IsBossTier`（蠕虫归主体后按 boss 旗/视同 boss 表/体节表判定），史莱姆王与硫磺火元素都在范围内。反馈人观察到的「不算」更可能就是上条 #144 的锁血漏洞（残酷史莱姆王有死亡演出；硫磺火元素是灾厄 boss，若有死亡动画同理）。文案本身仍可补一句「首领」的口径，随 #144 一起处理

## 三、联机

- [X] 0.9206 世纪之花联机依旧抽搐（#155，9/4，稳定复现；同因旧版：#137「9205 残酷 AI 世纪之花多人高速上下抽搐」、#113「残酷模式下世纪之花 AI 抽搐」未写版本）
  - 已修复核：0.9205 #51 的框架级收口（客户端在 `stateMachine.Update()` 前后快照/回填 `npc.velocity`，速度全权用同步值）随 0.9206 发布后仍报，且症状精确到「高速上下」。核对（9/5）：客户端上剩下的位置来源有两类。①悬吊弹簧本身：`UpdateSuspension` 的速度写入已收权威端，但它是高频竖向振荡，服务器每 10 帧同步一次速度，客户端拿一个采样值积分 10 帧再被下一包位置校正，竖向弹簧被「阶梯化」就是上下抖；②状态内的位置直写：`PlanteraPhaseTransitionState`/`PlanteraDespawnState` 的 `npc.position += 随机抖动`（表现用，各端各抖），`PlanteraTentacleAI`/`PlanteraHookAI` 逐帧 `npc.Center = boss.Center + ...` 跟主体走，主体一抖部件全抖。下一步：客户端不再积分同步速度，改为向服务器位置插值（或把弹簧改成客户端按锚点确定性重算、只同步锚点），随机抖动限本地表现幅度。需联机真机回归
  - 已修（2026/9/5）：对照灾厄 `VanillaNPCAIOverrides/Bosses/PlanteraAI.cs` 与 tML 源码后确证病根是**客户端运动模型与服务端不同**，#77/#51 两轮「客户端被动化」把差距拉到最大：原版/灾厄/本仓库钩爪部件用的都是「运动数学各端确定性积分、只门控生成与随机、决策点发包、不做心跳」，而本体被改成客户端定速积分 + 每 10 帧硬拉齐，服务端速度每帧在变（弹簧/摆动/拉弓/刹车），每次拉齐就是一次抖；原版 `netOffset` 平滑（2～4 px/帧衰减、上限 300）再把 6 Hz 的纠偏喂成锯齿，就是「高速上下」。此外状态内计时器/扑向/锁点都是不过网的 C# 字段，客户端每次换状态都新建实例归零，这是 8/13 首版也抖、被误诊为「两端都写」的真因。修法：回退 #77/#51，`UpdateSuspension` 与各状态速度写入全端跑；状态热字段（Timer/Counter/子阶段/扑向角/锁点/子相位计时/藤索方向）经 InnoVault 重制节点自带的 12 槽 `ai[]` 随 SyncNPC 同包到达（`PlanteraStateBase.WriteHot/ReadHot`，客户端在 NetSync 换态后与同态收包后收养）；换目标/扑出/刹车/激怒翻转/触地补 `netUpdate`，10 帧心跳降为 45 帧兜底；激怒由服务端裁决经位标志下发（它改移速与招式时长）；痉挛抖动改绘制偏移 `ShakeOffset` 不再碰 `npc.position`，死亡崩断横向冲量改确定性伪随机；钩爪/触手/藤索三处 `DrawVine` 起点补 `boss.netOffset`（原版画钩链就是这么做的）。全量编译 BUILD OK；法条收进 netcode 笔记 2.11。需联机真机回归（对照 #51 录像）
- [ ] 多人联机下鬼伞领域看不见水位线（#117）
  - 核对（9/5）：湖面镜面与水线泡沫由 `KikasaDomainRender.SetSharedParams` 按被观看的域（`KikasaDomain.Viewed`：自己的优先，否则范围内最近的他人领域）读 `LakeWorldY`/`RiseProgress` 画，这两个量都在域快照包里（`KikasaDomainPlayer` NetSend 写 `LakeWorldY`，收包回填 `RiseT`），静态上远端观看者是能画出水线的。没说是持伞者本机还是队友端看不见；若是队友端，与 0.9205 #14「多人表里切换长期无法使用」同属域快照未到/未刷新的联机簇。待回访哪一端、领域是刚开还是开了很久
- [ ] 多人联机鬼切领域会覆盖鬼伞领域特效（#122；同族：#138「鬼伞联机开领域的倒影很遮视野」）
  - 回指：0.9205 #30 备注「两个鬼伞一起开领域会抢背景」+ 0.9203 #88 拍板「鬼切领域天空每帧最高覆盖」。鬼切天空每帧 `Activate`、血湖天空同策，两人各开一域时本机屏幕只能显示一套，谁后激活谁在上。联机表现簇，是否给旁观端做「只看自己的域/看最近的域」属拍板；#138 归建议区
- [ ] 9205 多人模式下鬼湖无法切换到鬼雨，只能在鬼梦与鬼湖间长按切换；重开服务器后解决，触发点在流程推进到骷髅王（#129，未稳定复现）
  - 回指：0.9203 #92 / 0.9205 #14 表里切换簇。本轮 #154 在单人给出了同症的静态根因（翻转再受理戳 `flipReacceptAt` 是绝对帧戳、从不复位，见四·#154），联机端翻转是本机受理后转播，客户端的 `Main.GameUpdateCount` 在进出子世界/重连时同样会回跳。「重开服务器后解决」与「帧戳残留、重连重建 ModPlayer 即清零」吻合。随 #154 一并修，修后联机回归
- 联机建议：鬼切替死鬼频繁替死队友，希望替死免除队友（#160）。核对（9/5）：这是设计。替死鬼的替身选取顺序（`PlayerDeath.FindScapeTargetsFor`）明写为「1. 扩展屏幕范围内最近队友（推进倍率）→ 2. 其他玩家 → 3. 友善 NPC → 4. 全局最近敌怪」，队友是第一优先且专门推进倍率。要不要改顺序或加开关属拍板，归建议区

## 四、鬼伞

- [ ] 鬼伞进世界刷出来的伞（鬼域门伞）里出来后，无法开启深层领域（#154 前半，9/4）
  - 核对（9/5）：**静态确证**。「深层领域」即表里翻转（鬼雨形态）。`KikasaDomainPlayer.FlipDomain` 的受理条件里有 `Main.GameUpdateCount < flipReacceptAt` 即拒收（busy），`flipReacceptAt` 是翻转落定时写下的**绝对帧戳**（`GameUpdateCount + 90`，反馈四 #119 拍板的连点闸），全库只有这一处写、没有任何 ClearWorld/OnEnterWorld 复位。进出鬼雨世界后 `GameUpdateCount` 回跳（0.9203 #46 鬼域传送冷却锁死已证实这一点），只要回主世界后的帧数还没追上上次落定的戳，翻转就一直被当作「再受理间隔内」拒掉。与 0.9203 #46 完全同一类 bug，只是那次清理时漏了这一枚（它比那次修复晚加）。修法同款：挂 ClearWorld 复位 + 脏值自愈（剩余帧数超过一段间隔即视为跨世界残锁清零）。0.9203 #92 / 0.9205 #14 / #129 的表里切换报告大概率同源
- [ ] 鬼刀表层领域左右键一起按可以无敌冲刺攻击（#154 后半）
  - 核对（9/5）：说的是鬼切不是鬼伞（反馈人把两把并在一行）。神威疾走本就带无敌帧，「一起按」是疾走中接普攻；疾走的连携窗允许接招是设计（0.9203 #76 拍板过条件链）。要不要限制疾走期间出刀属拍板，暂归备查
- [ ] 鬼伞血湖领域的金焰会把召唤栏消耗一个（#84 整行改写，9/2；备注追问鬼手扒自己召唤物会不会丢伤害）
  - 核对（9/5）：「金焰」是召影转盘上那扇点燃/收回鬼火的按钮（`WheelWispName: 金焰`）。鬼火本体（`KikasaWisps/`）不生成任何弹幕、不碰 `slotsMinions`，鬼伞全部伞奴/鬼奴弹幕也都是 `minionSlots = 0`，静态上它不占召唤栏。「点金焰时少一个召唤物」的真实来源应是**打开转盘这个动作本身**：0.9203 #55 / 0.9205 #71 / 本表 #141 的「按住转盘让召唤物只剩两个/消失」簇（转盘触发单人世界冻结、召唤物上限回落的漏窗）。并入转盘簇，见八·#141
- [ ] 鬼伞上 buff 吞召唤物时有概率不吐出，表现为少一个召唤物但召唤栏不变（#121）
  - 同族：0.9205 #46/#84 拖入簇（鬼手抓自己的召唤物后重新出现是机制）。「召唤栏不变但少一只」是新数据点：被扣押的那只仍占栏、没有归还也没有再造。待真机
- [ ] 符纸归属规则新数据（#69 备注，9/4 于 9206 二次测试）：首位细雨符、次位冰雹符时才出现「细雨叠出霂后吃不到冰雹效果」；首位九霄符、次位细雨符、末位冰雹符时冰雹符特效完全不显示
  - 回指：0.9205 #7/#69「每滴墨只归一张符、靠左先认」的设计强度拍板仍未落。新数据把规则边界画得更清：冰雹排在细雨之后就分不到墨，排在第三位则一滴都分不到。拍板项照旧：改归属规则，还是文案写明「排位越靠后越少」
- [ ] 鬼伞只能将原版 BOSS 沉入湖底（#102）
  - 核对（9/5）：门槛不是按原版/模组分的。`KikasaBossGate.IsDefeated` 三层取或：注册 boss 认原版 downed 旗 → 灾厄 boss 认灾厄 downed 旗（含灾厄全 boss 名单）→ 其余模组 boss 查世界击杀台账；**本世界没击败过的 boss 一律转鞭笞**。反馈人大概率是拿首次遭遇的灾厄 boss 试的，或把鞭笞读成了「沉不下去」。预期行为，归档；若回访确认是已击败的灾厄 boss 也沉不下再开
- [ ] 拿着鬼伞时右键不能与埃特尼水晶互动开启下一波（#90，稳定复现）
  - 核对（9/5）：埃特尼水晶台是**物块交互**（`TileInteractionsCheck → DD2Event.RequestToSkipWaitTime`），原版在 `ItemCheck_ManageRightClickFeatures` 里已规定：智能交互高亮或悬停 NPC 时不走物品右键。鬼伞 `AltFunctionUse => true`，但持伞弹幕 `KikasaRainUmbrella` 读的是原始 `controlUseTile`（四处），右键一按伞就直接进倒撑蓄墨，不经原版那道让位判定；不过它也没有任何清 `controlUseTile`/`releaseUseTile` 的写入，静态上物块交互应当仍会发生。可能只是「伞在动作，波次也开了」被读成了没反应，也可能是倒撑态里某一帧把交互吞了。待真机：持伞右键水晶看波次计时是否变化；若真被吞，修法是伞的右键受理也加一道原版同款让位（悬停可交互物块/NPC 时不进倒撑）
- [ ] 喝血药后用重启，药水疾病在左上角的图标没了，但实际仍在，喝不了药（#132，灾厄+大修+更好的体验，稳定复现）
  - 核对（9/5）：**静态确证，三处重启同病**。`KikasaReset.ApplyLocalCleanse`、`CyberRestart.ApplyLocalRestore`、`HalibutReset` 都是遍历 buff 表把 `Main.debuff[]` 为真的全部 `DelBuff`，药水疾病是 debuff 会被删掉；但原版的喝药冷却是独立字段 `player.potionDelay`：只有 buff 在场时每帧把 `buffTime` 抄进 `potionDelay`（Player.cs 9613），buff 一删它就停在最后一次抄进去的值、自己每帧减 1（21478），直到走完为止。于是图标没了、`potionDelay > 0` 照旧挡喝药（`sItem.potion && potionDelay > 0`），与反馈逐字吻合。反馈里的「鬼切重启」应是绯嫁合卺重启走的鬼伞同款清洗。修法二选一：重启清 debuff 时同步 `potionDelay = 0`（重启即可再喝），或把药水疾病从清洗名单里排除（图标留着）。属语义拍板，倾向前者（重启本就是「洗掉一切」）
- 替死娃娃时而生效时而不生效（#145，偶尔）。核对（9/5）：替死鬼需要一个合法替身才能替死（`FindScapeTargetsFor` 四阶段都找不到就替死失败）；boss 战里 boss 与 boss 部件被 `IsBossTier` 排除，友善 NPC 与队友在 2400px 内才算，场上只剩 boss 时就「不生效」。设计使然，但玩家读不出来。文案可补「附近需要有能替你死的东西」，归文案

## 五、鬼切

- [ ] 鬼切深层领域肢解 boss 后，停滞 boss 的时间仍会在打开改铭台的时停里消耗（#157，灾厄+大修，稳定复现）
  - 核对（9/5）：**静态确证**。`OniDismember.UpdateAll`（挂 PostUpdateNPCs）对肢解锁定表 `lockEntries` 逐帧 `entry.Timer++`，到 `Duration` 即释放 NPC 的冻结租约；整段没有任何 `WorldFreezeSystem.IsActive` 门。改铭台/点鬼簿/图鉴的世界冻结只拦实体 AI，ModSystem 钩子照跑，所以停滞倒计时在时停里照常走完，关掉界面 boss 已经解锁。与 0.9205 #77（时停里 DoT 烧血）/ #78（点鬼簿卡时停降复苏值）同一类「世界冻结没有覆盖到的计时」，修法同款：冻结期跳过两张表的 `Timer++`（肢解碎片表 `Entries` 的演出计时也一并停，免得解冻后碎片瞬间归位）
- [ ] 时停框架簇新报告：在鬼切的处决时停里，终灾最后几阶段的黑洞能动（#150）；灾厄弹幕炼狱阶段打开鬼切改铭台的时停再关上后，灾厄的弹幕被留在空中不动（#151，均灾厄+大修，稳定复现）
  - 回指：0.9205 #17「切换役鬼的系统时停后 boss 弹幕悬停不恢复」（红茶注：疑特殊 ai 射弹时停恢复不稳）。#151 把复现面从白金星舰扩到至尊灾厄弹幕炼狱、从役鬼时停扩到改铭台世界冻结，坐实是框架级：`TimeFreezeProjectile` 冻结期拦 AI、握位置，解冻回填 `ResumeVelocity`，但每帧自算速度/位置的特殊 AI 弹幕（灾厄大量此类）被回填一个旧速度后并不会自己「续上」。#150 反过来是漏冻：世界冻结 `ShouldFreezeProjectile` 一律为真，能动的东西不是 Projectile/NPC 实体，至尊灾厄最后阶段的「黑洞」若是灾厄的屏幕着色/竞技场层就冻不住。两条并入 #17，待框架级方案
- [ ] 鬼切和灾熵的「作孽双子」召唤物每一击可以叠槽，从而无限处决（#146，熵，附图：处决大爆发同屏大量召唤物伤害数字；反馈人问是否有意为之）
  - 核对（9/5）：架势的全部涨点都在鬼切自己的刀击回调里（`OnComboHit`/`OnZanshinHit`/疾走弹反/处决击杀返还，`OnikiriPlayer`），全仓没有「任意弹幕命中即回架势」的通用钩。本机的灾厄熵源码与灾厄源码里都找不到「作孽双子」这个名字，它不是本机能读到的模组内容。若真能叠槽，只可能是那只召唤物在复刻/触发持有武器的命中回调（如复制手持武器攻击的仆从），属兼容备查；待回访它出自哪个模组、鬼切放下时它还能不能叠
- [ ] 在黑暗有背景墙处，鬼切挥刀后光亮消失时，背景墙透明一段时间再恢复（#94）
  - 核对（9/5）：挥砍链（`CrimsonRendSlash`/`CrimsonSweepSlash`/`CrimsonRendCleave`）对光照只做普通 `Lighting.AddLight`，没有碰墙体绘制。「光灭之后墙透明一会儿」更像挥砍的屏幕层残留：刀光后至的「撕开缝隙」与余韵粒子由 RenderHandle 在实体层之上直绘，暗处墙被原版按亮度剔除时，这层残留就在墙的位置上显出来，读作「墙透明」。待真机截图定位是哪一层（关闭刀光余韵是否消失）
- [ ] 鬼切的新手教程道具每次进游戏都会发一遍（#116）
  - 核对（9/5）：**静态确证**。道具是稽古符（`OniKeikoRune`）。`OnikiriTutorialPlayer.TickRuneReissue` 的补发巡检：婉拒过教程且从未完成、持有鬼切、**背包里没有稽古符**（`Player.HasItem` 只查背包，不查猪猪存钱罐/保险箱/箱子）就补发一枚，`runeReissued` 是会话内一次性闩，每次进世界重建。于是把符收进箱子或存钱罐、或者干脆丢掉的玩家，每次进世界都会再拿到一枚，「每次进游戏都会发一遍」就是这条巡检。设计意图是防符丢失锁死委托链，但判定面太窄。修法：改用 `HasItemInAnyInventory`（含各类银行）判定，并把「丢弃」当成明确意愿至少加一道确认或降低补发频率
- [ ] 9205 把樱流巡航绑在鼠标 4 号键，会和智能光标冲突：智能光标指向可右键物品时无法樱流，之前的版本没有（#87）
  - 回指：0.9203 #74/#76 拍板维持。樱流与疾走的输入口都过 `HasWorldInteractionInputBlock()`（智能交互高亮即拒收，防误触发），该门自 9202 起就在；「之前的版本没有」应是键位换到鼠标侧键后更容易在指向物块时触发。归档，与 #74 同口径
- [ ] 打开任务书与专属武器配件面板时有人物跑步声（#119）
  - 回指：0.9203 #75 / 0.9205 #35 冻结脚步声簇（声源尚未定位，转真机）。新数据点：任务书与 SHPC 配件面板也响，两者都是全屏界面，把相关性从「世界冻结类 UI」扩到「全屏界面」，回访时并案
- 不能召唤召唤物，鬼切是这样，鬼伞不知道（#107）。信息不足：没说在领域内还是领域外、什么召唤物；鬼梦里人人失能是设计（0.9203 十三 #21），鬼切领域没有召唤禁令。待回访
- 御鬼副作用太大，打伤害先被反噬死、打控制也先死（#120）。数值/机制愿望，归建议区

## 六、SHPC / 超梦 / 工业 / 义体

- [ ] SHPC 命中部分 boss 会使其变成绿色，末世星史莱姆会显示触发 bug、无法完成试炼（#98，常态+灾熵，附图：屏幕上灾劫的对话「你触发了一个不常见的 bug……重新召唤这个 Boss 并击败它……记得把 bug 报告给灾劫 Mod 开发组」「末世星史莱姆失去了对你的兴趣」）
  - 核对（9/5）：末世星史莱姆是灾劫（Catalyst）的 boss，图里的提示是灾劫自己的异常检测把 boss 收走了（它认为自己进入了不该有的状态），本模组试炼只在真死时入账，boss 被它自己收走自然不算。「变绿」是 SHPC 命中的表现层（骇入类着色）还是灾劫的状态色待分辨。兼容备查，需回访：用其它武器打末世星史莱姆是否也触发、绿色出现在命中哪一招之后
- [ ] 载入 9205 后选择义体手术，义体装备不上（#104，稳定复现）
  - 核对（9/5）：义体目录自 8/30 起无任何提交，9205 没改过这里。装备入口 `CyberwarePlayer.TryEquip` 有两道门会静默拒绝：①`ProfileInitialized`，联机客户端要等服务器下发档案，没到位一律拒装；②容量 `CanEquip`（已装容量 + 新件 `CapacityCost <= FixedMaxCapacity`）。待回访：单人还是联机、装的是哪件、已装几件；若单人且容量未满再开
- [ ] 末世星史莱姆的古圣金源套会把大脑义体的四个虚影样貌固定成该套装，换挡也不变，重开游戏才恢复（#133，灾厄+灾劫+大修，附图：四只虚影全穿同一套金紫套装；反馈人自评无所谓）
  - 核对（9/5）：大脑义体虚影按玩家外观拍快照绘制，灾劫套装若靠自绘层替换玩家贴图，快照就把它固定住了。低优兼容备查
- [ ] 嘉登的量子纠缠塔建造完 10 座任务完成了，但没有后续，便携式量子通信器显示只建了 9 座（#149）
  - 核对（9/5）：两边读数来源不同。委托完成是一次性写进故事数据的永久旗（`DeploySignaltowerNarrativeCheck.CheckQuestComplete` 在部署数 ≥10 持续 120 帧后写 `DeploySignaltowerQuestCompleted`，此后永远显示完成），通信器（PQCD）的 x/10 则是每帧按目标点 `IsCompleted` 实时统计，而信号塔 TP 的 `OnKill` 会把自己占的目标点撤回未完成（`UnmarkCompletionByIndex`）。任何一座塔在完成之后被拆/被毁，通信器就回落到 9/10 而委托仍是完成态，两边不再一致；塔 TP 的「占了哪个点」没有存档，重载后拆塔反而不会撤回。「没有后续」指完成演出（`QuestCompleteScenario → FirstMetTzeentch.Open()`）没出现，待回访是否拆过塔、完成瞬间在做什么
- [ ] 无灾厄模组时不会生成灾厄小行星与实验室，因此世界没有「荒野的风力发电机」，任务图谱「工业革命」及后续无法完成（#86，稳定复现）
  - 核对（9/5）：结论对了一半。风机配方确实押在「见过荒野风机」上（`WindGrivenGenerator` 配方条件 `UnderstandWindGriven`，由荒野风机 TP 发现时置真），任务「工业起步」又只认持有风机，所以世界里没有荒野风机就整条工业链锁死。但**无灾厄回退早就有**：8/26 起 `IndustrializationGen.ApplyPass` 在 `!CWRRef.Has` 时走 `IndustrialSkyIslandGen.Generate()`，程序化生成工业空岛承载荒野风机与 MK2（数量 =（2 + 世界尺寸档）× 密度倍率，常规 3~5 座，上限 20），落点在原版浮岛高度带、只受世界生成密度设置门控。反馈人的 9205 世界应该已有这条回退，所以要分辨：是空岛生成失败（120 次尝试 × 纯净天空判定全落空、密度设为绝迹），还是生成了但玩家没在天空层找到。待回访：世界尺寸、世界生成密度设置、是否全图探过天空层
- [ ] 无灾厄时因为没有武器充能系统，「能量剑」与「镭射手枪」无法正常使用，「特斯拉电磁塔」的护卫模式也没有效果（#88，稳定复现）
  - 核对（9/5）：**静态确证内容缺口**。充能全部走 `CWRRef` 对灾厄 `CalamityGlobalItem.Charge/MaxCharge/UsesCharge` 的反射（`GetItemCharge` 等），灾厄缺席时 `GetItemCharge()` 恒 0、`SetItemUsesCharge` 无效：能量剑 `HasCharge` 永假、终结斩不放；镭射手枪开火条件 `GetItemCharge() > 0` 永假；特斯拉塔护卫模式给手持武器充能的两处判定 `GetItemUsesCharge()` 永假，充电站同理。这三件与雪崩 M60（0.9205 #83）一样本意是无灾厄可用，缺一套自有充能回退（或无灾厄时不给配方）。拍板项：自建充能字段回退，还是无灾厄时隐藏这批物品
- [ ] 磁花的神匠重铸没有效果（#103，9205 发现）
  - 核对（9/5）：磁球方案 `GsMagnetSphere`（A 档：馈磁链涨磁通、白热过充泼弧、右键磁暴）自 8/28 起在库、走 ItemOverride 自动装载，实现完整；9205 前后没改过它。「没有效果」含义不明：面板没有神匠简述？持杖没有馈磁链？磁通不涨？待回访再定
- [ ] 击败无尽虚空后 boss 血条有概率不消失，退出重进世界也不恢复，只能等打下一个 boss 把它顶掉（#95，灾厄+大修+灾熵，不稳定）
  - 核对（9/5）：若用的是本模组赛博血条（`CyberBossBarStyle`）：条目按 NPC 槽位+类型跟踪，槽位失活或类型变化后 80 帧内收起，跨世界重进也会自愈。「重进世界仍在」说明有一只 `boss` 旗的 NPC 一直活着（灾厄无尽虚空/灾熵侧的残留实体），与 0.9205 #66「无尽虚空打完第一次暗能量后消失但一直存在」同源。待回访血条样式、生命探测器/boss 雷达是否还能探到它

## 七、任务书 / 世界 / 环境

- [ ] 新角色新地图刚进入，指导书里 BOSS 清单基本全完成，几秒后嘉登就来给任务；反复几个存档都一样，进入后什么都没做直接完成（#111；反馈人注 9203 左右没有，9205 打完一个存档再建时发现）
  - 核对（9/5）：静态找不到跨角色/跨世界的泄漏路径。任务进度（完成/解锁/目标计数）全部在 `QLPlayer.QuestProgress` 每角色存档里，boss 任务每帧按世界 downed 旗重算；嘉登的信号塔委托只在**本世界星流巨械已败**（`InWorldBossPhase.Downed29`）时登门。所以要在新世界里出现这两件事，新世界的击败旗必须已经是真。本模组唯一会往世界旗里写 true 的地方是子世界守卫的快照回填（`KiameGuard`/`KiyumeGuard`/`OldNetGuard`/`CybCourseWorldGuard` 等，进子世界前拍灾厄旗标、回主世界 OR 写回），但它们全部绑世界 UniqueId、世界不符整组丢弃，静态封得住。需回访：新角色是不是用「复制」建的（复制会带走任务存档）、新世界是不是复制或用了别的模组的进度同步、装了哪些模组、「指导书」指的是任务书还是 Boss Checklist
- [X] 无灾厄时任务图谱有两对节点重叠：「史莱姆皇后」叠「变废为宝」、「光之女皇」叠「自动装配」（#85，附图：无灾厄图谱，机械三王下方两格各叠着两个节点）
  - 已修复核：核算无灾厄坐标，回收机/装配台原挂在 (900,450) 的粉碎机西侧，正落在史莱姆皇后 (750,450) 与光之女皇 (750,600) 同格；9/2 15:03 的提交已把粉碎机链改挂「工业起步」西邻 (600,-150)（`CrusherQuest` 注释原文即记着这两处同格），随 0.9206 发布（0.9206 #10「调整部分任务节点排布」）。待无灾厄真机看图谱
- [ ] 一靠近微光就有 MC 产生黑曜石那样的声音，只能退出世界刷新（#143）
  - 核对（9/5）：残酷微光地带氛围（`Aetherglim`）的声音有三种：空灵风铃与远钟（Item26/Item35 一次性）、泡破（Shimmer1 一次性）、水下深引期间的低鸣循环（`AetherglimPlayer.UpdateHum`：`DD2_EtherianPortalIdleLoop` 压到 -0.82 音高、回调按 `humLevel < 0.01` 自停，离水/死亡/档位关闭都把它归零）。静态上唯一的循环音是有出口的，「只能退出世界刷新」的持续音对不上；也可能是原版微光自己的 `Shimmer` 声被大量微光转化触发。待回访：是否残酷、是否在微光水里、声音是持续还是频繁重复
- [ ] 环境瓶模组的速建房有时会生成潜行空间的炮塔替代木块，且无法破坏（#105）
  - 核对（9/5）：「潜行空间的炮塔」应是旧网的哨戒炮塔（`OldNetTurretICE` 为 NPC，物块层另有旧网建材）。它模按物块 ID 批量摆放时若拿到了本模组的物块 ID，就是它模侧的 ID 选取问题；「无法破坏」符合旧网建材的保护判定。兼容备查，需回访模组全名与生成的物块具体是哪个
- [ ] 一到开局空岛的绳子右边点点就自动隐身（#147，灾厄+大修）
  - 核对（9/5）：信息不足。「开局空岛」若指 SHPC 坠舱空岛，那里没有任何隐身机制；鬼切疾走隐身旗 `LocalPlayerHidden` 由疾走弹幕放行（0.9205 #19 已补切世界清理）。待回访截图与是否手持鬼切
- [ ] boss 列表记录不了脓蕾沙蟒（#152）
  - 核对（9/5）：Boss Checklist 已登记脓蕾沙蟒（`FssChecklist` 注册 `FssHead` + 击败旗 `FssWorldFlag.DownedFesterSerpent`，进度值 7.5），击败旗在 `FssHead.OnKill` 里经 `SetEventFlagCleared` 置真，死亡演出终幕走 `life=0; checkDead()` 会到 OnKill。「记录不了」含义待回访：图鉴里没有条目、击败后不打勾、还是别的 boss 列表（灾厄/它模）
- 残酷模式惊岩落石概率过高、没有提示、垂直挖矿也触发、零帧起手，怀疑设计问题（#135）。残酷环境机制的强度拍板，归建议区备查；`AMBIENCE.md` 的预告/可逃法条要求有前摇，「零帧起手」「没有提示」若属实是违反自家法条，值得真机看一眼

## 八、兼容

- [X] 忍法克苏鲁闪耀靴冲刺与仪轨集环冲突：不飞起来就冲刺不了（#93，9205 新 bug；红茶注：已尝试修复，占位）
  - 已修复核：0.9206 #24「同时佩戴忍法克苏鲁闪耀靴与仪轨集环时地面双击冲刺被空闩卡死」，修复提交 9/2 20:18（`NinjaCthulsparkBoots.cs` + `RiteRing.cs`）。待 9206 真机
- [X] 9205 灾厄的荒漠灾虫、腐巢意志等 boss 挂上蚀界之颚的减益后贴图变成马赛克（#100；红茶注：已尝试修复，占位）
  - 已修复核：同一提交（9/2 20:18）重写了 `BRelicMawEtch.fx` 与 `MawCorrosionNPC` 的批次闩锁，就是 0.9206 #25。注意 0.9206 日志把它写成了「饕餮之喉蚀刻」，实际改的是蚀界之颚的酸蚀刻痕着色器，日志措辞有误。待 9206 真机
- [X] 黑屏簇：带鬼切去另一个世界点同步后，鼠标悬停鬼切面板直接黑屏，全删模组重装仍在，解锁全部鱼类知识的道具也黑屏（#115）；光标对准物品栏鬼切时黑屏（#118，附图：整屏全黑只剩「鬼切」二字与一颗星）；9205 按 esc 开背包鼠标移到鬼切图标黑屏，只有动态彩色字偶尔出现，鬼伞和 SHPC 不会（#142，有录像 2026-09-03 19-55-49.mp4）
  - 已修复核：三条都是 0.9203 #86 同一成因（灾厄燃金稀有度的名字特效在提示框绘制期间租全屏 RT 切换渲染目标，切回时后备缓冲被清黑）。#115 点名的「解锁全部鱼类知识的道具」是海洋知识百科全书，9205 里同样用燃金稀有度，与鬼切是同一批受害者。0.9206 #2 自有稀有度落地后鬼切改走 `OnikiriLegendRarity`、百科全书改走 `GiltRarity`（9/2 14:13 提交），物品名特效全部在当前批次直绘不切 RT，这条黑屏入口整个消失。待 9206 真机悬停鬼切/百科全书；#115 提到的「跨世界同步」只是触发时恰在看面板，与同步功能无关
- [ ] 炼狱模式 + 炼狱大师传奇适配补丁同开时，开死神永生或其他大修难度，Boss 血量不变；9202/9203/9205 都有（#91）
  - 同因：0.9203 #53（兼容备查，两边都在 SetDefaults 期改血量、炼狱后写生效）。本条补充「9205 仍在」，未动
- [ ] 大师死亡修罗地狱难度被史莱姆之神击杀后，两个圣卫持续坠落，掉出地图外也不停（#134）
  - 同因：0.9203 #73（本模组对史莱姆之神零接管，坠地主体疑灾厄/灾熵侧）。本条数据点：单纯「玩家死后圣卫坠落」在修罗档也复现，未动
- [ ] 开神工开物并喝了豆奶后镰刀无法收割干草（#136，灾厄+大修+灾熵）
  - 同因：0.9203 #70（神匠镰刀方案自调 `CutHayTile`，疑挥砍窗未覆盖或它模改动画错位；「豆奶」为它模物品）。本条补充稳定复现，仍待真机
- [ ] 转盘吞召唤物簇：拥有召唤物时打开技能切换轮盘就会让召唤物消失只剩一只（#141，灾厄+熵+法狗，有录像 video_20260903_172903.mp4；反馈人注影响较大）
  - 同因：0.9203 #55 / 0.9205 #71 转盘簇（世界冻结拦掉 `UpdateEquips` 后上限回落，已有快照回写护栏仍有漏窗）。第三条独立报告且有录像，#84 改写后的「金焰消耗召唤栏」也归此。建议优先按录像真机定位
- 鬼切与灾熵作孽双子叠槽（#146）、SHPC × 灾劫末世星史莱姆（#98）、环境瓶速建房（#105）、灾劫套装固定义体虚影（#133）、灾熵 Apsychos BossRush（#158）见各模块条目，均列兼容备查

## 九、建议与愿望（不立目，备查）

- 优化开鬼域进鬼湖时视野变清晰，下矿开鬼域看不见、遇怪需要反复开关（#126，反馈人自注是建议）
- 把鬼刀样子改小一点；改完后整体挥舞手感像塑料刀、硬邦邦（#131）。归 `melee-slash-choreography` 口径的手感反馈，与 0.9203 #24 挥砍重做相关，留档
- 鬼伞联机开领域的倒影很遮视野（#138；与 0.9205 #30/#65 同）
- 希望鬼伞那边增加「可拉入湖中的 boss」图鉴（#140）。湖心景图鉴目前只列能沉的注册 boss，扩到「门槛已过的全部 boss」属内容愿望
- 难度调节可选择是否启用新 AI，不想被大修 AI 覆盖其他 AI（#156）。残酷模式已是世界级开关，愿望是更细粒度的逐 boss 开关
- 御鬼副作用太大（#120）
- 替死鬼免除队友（#160，设计如此，见三）
- 残酷惊岩落石强度（#135，见七）

## 十、无效 / 设计如此 / 提问 / 信息不足（留档）

- #92 老大视频里鬼切的无限肢解做进游戏了吗：表内提问，非反馈
- #112 鬼切重铸一直是神级：红茶注「关闭灾厄重铸」；传奇武器不走重铸
- #102 鬼伞只能沉原版 boss：门槛按「本世界击败过」不按来源，见四
- #145 替死娃娃时而不生效：需有合法替身，设计使然，见四（文案可补）
- #107 不能召唤召唤物：信息不足，见五
- #147 开局空岛自动隐身：信息不足，见七

---

> 清单7 新增行里 #101、#153 缺失，#55、#72 仍缺；#76 仍仅时间戳。时间戳列 #93「45171」、#116「44076」、#134「38963」、#139「47364」为明显错年的序列值，按相邻行与内容视作 9/2–9/3 报告。
>
> 留有录像可回访：#141（video_20260903_172903.mp4）、#142（2026-09-03 19-55-49.mp4）；#11/#39/#45/#48/#51 的旧录像见 0.9205 备注。
>
> 静态确证、可直接进修复批次的条目：#132 药水疾病残留（三处重启补 `potionDelay = 0` 或排除药水疾病）、#154 翻转再受理帧戳（挂 ClearWorld 复位 + 脏值自愈，顺带回归 #129 / 0.9205 #14 / 0.9203 #92）、#157 肢解计时不受世界冻结（两张表加冻结门）、#116 稽古符补发只查背包（改 `HasItemInAnyInventory`）、#144 刀縁了结在锁血 boss 上永不结算（真死回填入口，参照 0.9203 #44 腐环修法）、#158 BossRush 期间沉溺（转鞭笞或拒收）。#88 无灾厄充能与 #86 荒野风机回退失效属内容/拍板项。
>
> 0.9205 遗留未决项继续在 [0.9205.md](0.9205.md) 内跟踪，0.9203 更早未决项见 [0.9203.md](0.9203.md)；本表把其中表里切换（0.9205 #14 / 0.9203 #92 ↔ #154/#129）、冻结脚步声（0.9205 #35 ↔ #119）、转盘簇（0.9205 #71 ↔ #141/#84）、时停框架（0.9205 #17 ↔ #150/#151）、死亡追踪（0.9205 #3/#12 ↔ #106/#124）、饕餮消失（0.9205 #24 ↔ #127/#89）已在对应条目并案。
